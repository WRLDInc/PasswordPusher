# Password Pusher Environment Configuration Example
# Copy this file to .env and customize the values

# Database Configuration (PostgreSQL)
POSTGRES_USER=pwpush_user
POSTGRES_PASSWORD=change_this_secure_password
POSTGRES_DB=pwpush_db
DATABASE_URL=postgres://pwpush_user:change_this_secure_password@db:5432/pwpush_db

# Security - Generate a new secret key for production
# Run: openssl rand -hex 64
SECRET_KEY_BASE=generate_your_own_secret_key_here

# Rails Environment
RAILS_ENV=production

# Port Configuration
PORT=5100

# Application Features
PWP__ENABLE_LOGINS=false
PWP__ENABLE_FILE_PUSHES=true
PWP__ENABLE_URL_PUSHES=true

# Password Settings
PWP__PW__EXPIRE_AFTER_DAYS_DEFAULT=7
PWP__PW__EXPIRE_AFTER_VIEWS_DEFAULT=5
PWP__PW__RETRIEVAL_STEP_DEFAULT=true

# Host Configuration (update with your actual domain)
PWP__HOST_DOMAIN=pwpush.yourdomain.com
PWP__HOST_PROTOCOL=https

# Mail Configuration (required if enabling logins)
# PWP__MAIL__SMTP_ADDRESS=smtp.example.com
# PWP__MAIL__SMTP_PORT=587
# PWP__MAIL__SMTP_USER_NAME=your-smtp-username
# PWP__MAIL__SMTP_PASSWORD=your-smtp-password
# PWP__MAIL__SMTP_AUTHENTICATION=plain
# PWP__MAIL__SMTP_STARTTLS=true
# PWP__MAIL__MAILER_SENDER="PasswordPusher" <noreply@yourdomain.com>
